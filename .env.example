# Optional paid API tools read environment variables, not this file automatically.
OPENAI_API_KEY=
GEMINI_API_KEY=
# Optional executable overrides (otherwise PATH is used).
# FFMPEG=/path/to/ffmpeg
# FFPROBE=/path/to/ffprobe
